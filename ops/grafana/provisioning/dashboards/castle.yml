apiVersion: 1

providers:
  - name: "castle-runtime"
    orgId: 1
    folder: "Castle"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
