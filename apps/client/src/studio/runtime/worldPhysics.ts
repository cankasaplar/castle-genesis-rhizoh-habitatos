/**
 * World Physics Layer (WPL) — substrate for time dilation, memory decay,
 * entity interaction rules, and causal graphs. Live slice: `StudioKernelState.worldPhysics`.
 * Extend here as behaviors graduate from ad-hoc simulation into shared physics.
 */
export const WORLD_PHYSICS_LAYER_VERSION = 0;
