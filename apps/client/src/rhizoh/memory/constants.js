/** Unutma eğrisi: gün başına λ (Δt gün cinsinden). */
export const RHIZOH_MEMORY_LAMBDA_PER_DAY = 0.045;

/** Disk + meta’da tutulacak maksimum ağırlıklı tur. */
export const RHIZOH_WEIGHTED_MEMORY_CAP = 40;

/** Varsayılan LLM bağlamına giren üst sınır. */
export const RHIZOH_MEMORY_RETRIEVAL_DEFAULT_LIMIT = 14;

/** Semantic wide pool — physics collapse sonrası kesilir (pre-filter değil). */
export const RHIZOH_MEMORY_CANDIDATE_POOL = 52;
