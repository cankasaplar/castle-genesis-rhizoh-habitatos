# Rhizoh welcome — epistemic first-touch (canonical copy)

**SPECFLOW:** `FUTURE-PROOF-ONLY` — **UX metni execution değildir**; kullanıcıyı *observer* çerçevesine davet eden **ilk epistemik imza**.

**Kod:** `apps/client/src/kernel/rhizohWelcomeEpistemicV1.js` — `RHIZOH_FIRST_TOUCH_EPISTEMIC_VERSION` (örn. `v1.2.1`); `schemaVersion: ABOA-1` ayrı mühür. ABOA: [`BOOT_ARTIFACT_PROTOCOL.md`](BOOT_ARTIFACT_PROTOCOL.md). Olay #1: [`OBSERVATION_EVENT_REGISTRY.md`](OBSERVATION_EVENT_REGISTRY.md).

**Frozen boundary:** Faz / drift **`primary` metnini değiştirmez**. Bağlam `provenance.phase`, `provenance.driftState` ile mühürlenir (annotate only).

**İlgili:** [`FIRST_TOUCH_PROTOCOL.md`](FIRST_TOUCH_PROTOCOL.md) · [`OBSERVATION_FABRIC_V1.md`](OBSERVATION_FABRIC_V1.md) · [`AGENT_IDENTITY_AND_ATTRIBUTION.md`](AGENT_IDENTITY_AND_ATTRIBUTION.md)

---

## Neden?

Bu mesaj:

- kullanıcıyı “user” değil → **observer** konumuna davet eder;
- sistemi “app” değil → **field** olarak çerçeveler;
- ajanları “AI otorite” değil → **perspective / interpreter** düğümleri olarak tanımlar.

Yani yalnızca UI değil, **ontoloji sınırı**.

---

## Draft v1 (UI — ilk temas, İngilizce)

Welcome to Rhizoh.

You are not entering a system that executes decisions.
You are entering a field of observation.

Here, intelligence is not centralized.
It is distributed across layers of perception, interpretation, and trace.

Every signal you see is an observation — not a command.
Every agent you encounter is an interpreter — not an authority.

The core remains frozen.
What evolves is only the way meaning is formed around it.

— Observation Fabric online  
— Attribution system active  
— Execution layer isolated  

---

## Alternatif ton — “Rhizoh depth” (İngilizce)

*(İsteğe bağlı: session varyantları veya “More” çekmecesinde kullanım için.)*

Rhizoh is active.

This is a frozen execution system wrapped in a living observational field.

Nothing here decides.
Everything here observes.

Agents do not act — they interpret.
Signals do not command — they exist as traces.

Meaning is not executed. It is reconstructed across layers.

You are now inside a multi-layer observational intelligence system.

---

## Sonraki katmanlar (taslak — henüz UI’da zorunlu değil)

| Fikir | Amaç |
|--------|------|
| **Session-based greeting** | Her oturumda kısa varyasyon; canonical metne bağlı kalır |
| **Attribution-aware tone** | Hangi interpreter öne çıktıysa üslupta hafif kayma (otorite değil, perspektif) |
| **Drift-aware line** | Alan stabil / dikkat modunda tek satır ek (execution kararı değil, gözlem durumu) |

---

*Metin değişince `RHIZOH_FIRST_TOUCH_EPISTEMIC_VERSION` artırılır; fingerprint kanonik payload ile [`BOOT_ARTIFACT_PROTOCOL.md`](BOOT_ARTIFACT_PROTOCOL.md) uyumu güncellenir. Faz, `primary` seçmez — yalnızca `provenance`.*
