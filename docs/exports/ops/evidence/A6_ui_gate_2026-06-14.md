# A6 — UI read-only decision gate

**Date:** __________  
**Tester:** Can Kasaplar

## Status: PENDING — cohort screen not seen in 2026-06-14 smoke

Founder ingress flow: language → legal → **app** (cohort step skipped).

Likely cause: `sessionStorage` key `rhizoh_cohort_gate_decision_v0.1` already set from earlier visit in same session, or cohort gate completed server-side.

## How to trigger cohort screen (no terminal)

1. Chrome → `rhizoh.com` → **F12** → **Application** (Uygulama)
2. **Session storage** → `https://www.rhizoh.com`
3. Delete key: `rhizoh_cohort_gate_decision_v0.1`
4. **Local storage** → delete `rhizoh_legal_access_ack_v0.1` (optional — forces full re-entry)
5. Hard refresh (`Ctrl+Shift+R`) or new Incognito window
6. Complete language + legal again → **cohort screen** should appear before app

## Expected cohort UI (PASS criteria)

- [ ] Title like closed admission / beta cohort invite
- [ ] **Accept** and **Decline** buttons only
- [ ] **No** sliders, scores, or live engine output on this screen

## Screenshot needed

- `A6_cohort_screen.png` in this folder

## Code reference (already PASS in CI R5)

`ClosedAdmissionCohortScreen.jsx` → `completeCohortGateNoOpV0` only.

## Verdict

- [ ] PASS → `A6_ui_readonly_gate: true`
- [x] PENDING — awaiting cohort screenshot
