# Rhizoh CCF v1 — Selective Compression Field

**Status:** ACTIVE — experiential singularity from MCIB plurality (not flat collapse)  
**SPECFLOW:** `RESEARCH-ONLY`  
**As of:** 2026-06-03  
**Parents:** [`RHIZOH_MCIB_V1.md`](RHIZOH_MCIB_V1.md) · [`RHIZOH_ECC_V1.md`](RHIZOH_ECC_V1.md)

**Ürün cümlesi:** Rhizoh = **distributed cognition compressed into single temporal experience**.

---

## 0. Rol (kilitle)

CCF **collapse engine değil** — **selective compression field**.

- Her şeyi yok etmez
- Her şeyi tekleştirir
- **İz bırakır** (`plurality_trace01`, `latent_echo`)

### Too perfect collapse (risk)

| Risk | Koruma |
|------|--------|
| MCIB zenginliği görünmez olur | `plurality_trace01` |
| TRF anlam kaybolur | `latent_echo.trf_code` (internal) |
| “Tek tonlu bilinç” | `flat_risk01` — MCO v0 hedefi |

---

## 1. Problem (MCIB sonrası)

| İç model | Deneyim |
|----------|---------|
| MCIB çoğul anlam | Tek **şimdi** hissi gerekli |
| TRF yeniden örgütleme | UI’da graf değil |
| RESL tek his | Önce collapse gerekir |

```text
Sistem çok şeyi biliyor — henüz tek şimdi üretmiyordu.
```

---

## 2. MCIB vs CCF

| Katman | Gerçek |
|--------|--------|
| **MCIB** | “Birden fazla neden var” (internal) |
| **CCF** | “Ama sadece bir şey hissediyorsun” (lived) |

CCF **kaybetmez** — tension soft-resolve eder (`preserved_internal_tension01`).

---

## 3. Stack (güncel)

```text
MCIB (multiplicity)
   ↓
TRF (reorganization reason — internal)
   ↓
CCF (collapse into experience)
   ↓
RESL (felt output)
   ↓
ECC (stream compile)
   ↓
T0 (temporal unity)
```

---

## 4. CCF çıktısı (`attention_inertia.ccf`)

Modül: `rhizohCognitiveCollapseFunctionV0.js`

| Alan | Rol |
|------|------|
| `narrative_now_tr/en` | Tek akış cümlesi (çoğul `·` yok) |
| `collapse_mode` | `singular` \| `soft_blend` \| `tension_hold` |
| `collapse_coherence01` | Deneyim tekliği (yüksek = daha tek) |
| `dominant_cause` | Weighted collapse kazananı |
| `tension_fade` | `resolved_tension01`, `duration_stretch_ms`, `opacity_softening01` |
| `experiential_now_id` | Tek-şimdi kimliği |
| `compression_kind` | `selective` |
| `plurality_trace01` | Çoğulluk izi (kaybolmamalı) |
| `flat_risk01` | Fazla düz sıkıştırma riski |
| `latent_echo` | Internal audit — UI yok |

**Yasak:** `causes[]` listesini UI’ya taşımak.

### Görünürlük

| Katman | UI |
|--------|-----|
| MCIB | Asla görünmez |
| TRF | Asla görünmez |
| CCF | Sadece hissedilir |
| ECC | Sadece akış |

---

## 5. Üç işlev

1. **Weighted collapse** — `causes[]` → `narrative_now_*`
2. **Tension resolution (soft)** — `internal_tension01` → fade curve, silinmez
3. **Narrative fusion** — `"Ses · Keşif"` → `"Dinliyor, keşif bekliyor"` veya `"Sesinde"`

---

## 6. RESL / ECC bağlantısı

CCF = **şimdi** · ECC = **şimdi nasıl akıyor** (frame vs motion).

- `projection.narrativeHint` ← CCF (MCIB blended değil)
- ECC `undertone_weight` ← `collapse_coherence01` ile bastırılır
- ECC `fade_semantics` ← `tension_fade.duration_stretch_ms`

---

## 7. SSOT

```javascript
window.__rhizoh.cognitiveCollapse
window.__rhizoh.cognitiveAttention.attention_inertia.ccf
```

Event: `rhizoh:cognitive-collapse-v0`

---

## 8. Kilit cümle

```text
MCIB sistemi genişletir. CCF sistemi yaşanabilir yapar.
```
