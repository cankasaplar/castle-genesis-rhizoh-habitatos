# Rhizoh Epistemic Soft Identity v0

**Status:** DRAFT · `RESEARCH-ONLY`  
**Spine:** WRITE-FREE memory · soft continuity via state reconstruction

---

## Principle

| | |
|--|--|
| Hard memory | ❌ |
| Soft continuity | ✔ |
| Identity SSOT | ❌ |
| Pattern echo | ✔ statistical only |

> The system does not store users; it reconstructs return probability through non-causal observational geometry.

---

## Visitor epistemic fingerprint

Reconstructed live from observation plane (B) — **not stored as memory**.

```javascript
visitor_epistemic_fingerprint = {
  session_signature: "eps_<hash>",      // hash(visit_events)
  attention_pattern: { map, chess, castle, media, chat },
  return_likelihood: 0.0–0.98,
  stability_index: 0.0–1.0,
  reconstructionOnly: true,
  isMemory: false
}
```

**Sources:** `observerTrace` entries + `visitorTrace` path echo.

---

## Modes

| Mode | Behavior |
|------|----------|
| **A) Strict Anonymous** | No return recognition (`familiarity: 0`) |
| **B) Epistemic Familiarity** (default) | Pattern similarity — behavior shape, not identity |

```javascript
window.__rhizoh.epistemicReturnField.setMode("epistemic_familiarity");
```

---

## Observation surfaces

Map · Castle · MediaPlayer = **observation surfaces**

- Produce attention trace ✔
- Do not affect causal graph ❌
- Do not write identity lifecycle ❌

---

## Related

- [`RHIZOH_EPISTEMIC_RETURN_FIELD_V0.md`](RHIZOH_EPISTEMIC_RETURN_FIELD_V0.md)
- [`RHIZOH_READ_ONLY_HOOK_V0.md`](RHIZOH_READ_ONLY_HOOK_V0.md)
- Runtime: `visitorEpistemicFingerprintV0.js` · `epistemicReturnFieldV0.js`
